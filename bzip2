#!/bin/bash
# Command is autogenerated by ./dev/generate-command.sh
set -eufo pipefail
cd "$(dirname "$0")"
arch="$(uname -m)"
bin="./bin/$arch/busybox"
if [[ ! -f "$bin" ]]; then
    echo "Unknown arch: $arch" >/dev/stderr
    exit 1
fi
"$bin" bzip2 "$@"
